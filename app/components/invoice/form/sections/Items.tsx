"use client";

// Components
import { SelectPlan } from "@/app/components";

const Items = () => {
    return (
        <section className="flex flex-col gap-6 w-full">
            <SelectPlan />
        </section>
    );
};

export default Items;
