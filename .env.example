# Shree Eniyaa Chitfunds (P) Ltd. Environment Variables - FRONTEND

# ========================================
# BACKEND API CONFIGURATION (REQUIRED!)
# ========================================
# Production backend URL (update this with your actual backend server URL)
NEXT_PUBLIC_API_URL=https://your-backend-api.com
# Development backend URL (local)
# NEXT_PUBLIC_API_URL=http://localhost:5000

# Database (Only needed if running backend locally with Next.js API routes)
# MONGODB_URI=mongodb://localhost:27017/chitfund

# Authentication (Frontend doesn't need JWT_SECRET, backend handles it)
# JWT_SECRET is only needed on backend

# App Configuration
NEXT_PUBLIC_APP_NAME=Shree Eniyaa Chitfunds (P) Ltd.
NEXT_PUBLIC_APP_URL=https://your-frontend-domain.com

# Firebase Configuration (optional - for web push notifications)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# Development
NODE_ENV=production

# ========================================
# IMPORTANT NOTES:
# ========================================
# 1. NEXT_PUBLIC_API_URL should point to your backend server
# 2. Backend handles: Database, JWT, SMS, Email
# 3. Frontend only needs: API_URL and Firebase (optional)
# 4. Never expose JWT_SECRET or database credentials in frontend
# ========================================